import { FC } from "react";
import { PageButton } from "../components/layout/PageButton";

/**
 * 都道府県別表示ページ.
 */
export const Prefectures: FC = () => {
  return (
    <>
      <PageButton />
      都道府県別
    </>
  );
};
